<h1>ショッピングカート</h1>

<table class="table">

	<thead>
		<tr>
			<th>商品名</th>
			<th>単価(税込)</th>
			<th>数量</th>
			<th>小計</th>
      <th></th>
		</tr>
	</thead>

	<tbody>
    <% @cart_items.each do |cart_item| %>
      <tr>
        <th><% cart_item.name %></th>
        <td>
          <% tax_price = cart_item.price.to_i * 1.1 %>
          <% cart_item.price %>
        </td>
        <td><% cart.item.quantity %></td>
        <td><%= link_to "削除", genre_path(genre), method: :delete %></td>
      </tr>
    <% end %>
	</tbody>

</table>
<% link_to "買い物を続ける", products_path %>