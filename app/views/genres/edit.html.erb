<h1>ジャンル編集画面</h1>

<%= form_with model: @genre do |f| %>
  <%= f.label :ジャンル %>
  <%= f.text_field :name %>
  <%= f.radio_button :is_active, :enable %>有効
  <%= f.radio_button :is_active, :disable %>無効
  <%= f.submit "変更を保存する" %>
<% end %>
